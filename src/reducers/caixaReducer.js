const initialState = {
    // Defina o estado inicial para o módulo de caixa
  };
  
  const caixaReducer = (state = initialState, action) => {
    switch (action.type) {
      // Adicione cases para ações específicas aqui
      default:
        return state;
    }
  };
  
  export default caixaReducer;
  